export interface HotVaultCardProps {
  nftImage: string;
  collection: string;
  nftName: string;
  floorPrice: string;
  totalShares: number;
  soldShares: number;
  nftId: string; // for routing to NFT details
}
